<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="grmasa.com.open_light">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name=".MainActivity" />
        <activity android:label="@string/app_name" android:name=".device_setup.Setup_step1" />
        <activity android:label="@string/app_name" android:name=".device_setup.Setup_step2" />
        <activity android:label="@string/app_name" android:name=".device_setup.Setup_step3" />
        <activity android:label="@string/app_name" android:name=".device_options.Device" />
        <activity android:label="@string/app_name" android:name=".device_options.Device_error" />
        <activity android:label="@string/app_name" android:name=".room_options.setup.Room_Setup_Step1" />
        <activity android:label="@string/app_name" android:name=".room_options.setup.Room_Setup_Step2" />
    </application>

</manifest>